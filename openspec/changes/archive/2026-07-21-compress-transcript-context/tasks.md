## 1. 单条消息长度上限与截断标记（Go 侧）

- [x] 1.1 在 `backend/internal/agent/prompt_composer.go` 中新增单条消息字符长度上限常量，并实现"超长截断 + 追加省略标记"的辅助函数
- [x] 1.2 为该辅助函数补充单元测试：覆盖"未超长原样保留""恰好等于上限""超长截断并带标记"三类场景

## 2. 新旧分层呈现（Go 侧）

- [x] 2.1 设计并实现"全文层 / 摘要行层"的分层逻辑：确定窗口内哪些消息属于全文层、哪些降级为摘要行层
- [x] 2.2 实现摘要行层的呈现格式（发言人 + 类型 + 内容前缀），确保不泄漏完整正文
- [x] 2.3 更新 `formatTranscriptBlock`，按分层结果分别调用全文格式化和摘要行格式化
- [x] 2.4 为分层逻辑和摘要行格式化补充单元测试：覆盖"窗口内消息全部落在全文层""部分消息降级为摘要行""空 transcript"等场景

## 3. 总体字符预算与回收（Go 侧）

- [x] 3.1 新增 transcript 总体字符预算常量和最小保留消息条数常量
- [x] 3.2 实现预算回收逻辑：分层呈现后若总长度超预算，从最旧消息开始进一步压缩或丢弃，直至满足预算或达到最小保留条数
- [x] 3.3 为预算回收逻辑补充单元测试：覆盖"未超预算不触发回收""超预算触发部分回收""回收至最小保留条数仍超预算时停止丢弃"三类场景

## 4. Python 侧等价实现（生产执行路径）

- [x] 4.1 在 `deepagent/src/agent_runtime/prompt.py` 中新增与 Go 侧一致的截断/分层/预算常量（同名量级：全文层条数、单条字符上限、摘要前缀长度、总预算、最小保留条数）
- [x] 4.2 实现 `format_transcript` 的等价压缩逻辑：单条截断 + 省略标记、新旧分层、总预算回收，行为与 Go 侧在相同输入下逐字节一致
- [x] 4.3 为 Python 侧压缩逻辑补充 `pytest` 单元测试，覆盖与 Go 侧对应的场景（未超长/恰好等于上限/超长截断、全文层/摘要层分界、预算未超/超预算回收/触达最小保留条数）

## 5. 跨语言一致性与集成验证

- [x] 5.1 在 `NewMentionPromptContext` / `NewGuidedPromptContext` 及 `formatTranscriptBlock` 的调用链路上确认 `PromptContext.Transcript` 到最终提示词文本的拼装路径按预期工作（Go local 回退路径）
- [x] 5.2 更新 `proto/agent_runtime/v1/testdata/prompt_golden.json`，新增覆盖"超长消息""长会话触发分层/预算回收"的用例
- [x] 5.3 运行 `go -C backend test ./...`（含更新后的 `prompt_golden_test.go`）确认 Go 侧黄金测试和既有测试全部通过
- [x] 5.4 运行 Python 侧 `pytest deepagent/tests/test_prompt_golden.py`（及新增单测）确认 Python 侧黄金测试通过，与 Go 侧输出逐字节一致
- [ ] 5.5 人工检查一次真实房间的长对话场景（本地或测试环境，走 `grpc` 传输的生产路径），确认压缩后的提示词仍能让 Agent 保持对话连贯，不产生因截断/降级导致的明显语义错误
